WEB-INF 밑에 있는 jsp는 바로 실행할 수 없다.
WEB-INF 밑에 있는 jsp는 컨트롤로에 의해서만 실행할 수 있다.

webContent == webapp 밑에 있는 jsp는 바로실행할수있다.
프로젝트명 우클릭..run As-Run on SErver 주소창에 http://localhost:8080/ex/WEB-INF/views/member/memberView 
 /WEB=INF/views/member/memberView.jsp 이곳으로 이동한다.
